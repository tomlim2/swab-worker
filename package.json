{
  "name": "weekly-notification-worker",
  "version": "1.0.0",
  "description": "Cloudflare Worker for weekly Slack notifications",
  "main": "src/index.ts",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "deploy:dev": "wrangler deploy --env development",
    "deploy:prod": "wrangler deploy --env production"
  },
  "dependencies": {
    "@cloudflare/workers-types": "^4.20241106.0"
  },
  "devDependencies": {
    "typescript": "^5.0.0",
    "wrangler": "^3.114.11"
  }
}
